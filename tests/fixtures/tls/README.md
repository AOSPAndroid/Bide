This self-signed localhost certificate and PUBLIC TEST KEY are used only by the HTTPS server integration test. Never use them to host bide. They are not installed in any operating-system trust store; the test client trusts the certificate for one request. Production HTTPS needs a certificate issued and trusted by your work IT, with its corresponding private key kept outside the served site and source repository.
